---
name: c-setup
description: Установка ai-tools в текущий проект или глобально
---

# AI Tools Setup

Помоги пользователю установить компоненты ai-tools.

## Шаг 1: Определи контекст

Спроси пользователя:
1. Установить глобально (`~/.claude/`) или в текущий проект (`.claude/`)?
2. Какие компоненты нужны?
   - MCP серверы
   - Команды
   - Скиллы
   - Всё

## Шаг 2: Путь к ai-tools

Определи путь к репозиторию ai-tools:
- Проверь `~/projects/ai-tools`
- Проверь текущую директорию
- Спроси пользователя если не найден

## Шаг 3: Установка MCP

Если выбраны MCP серверы:

1. Проверь существование `settings.json` в целевой директории
2. Если существует — предложи:
   - Заменить (с бэкапом)
   - Добавить отдельные серверы
   - Пропустить
3. Скопируй или обнови конфиг

Минимальный рекомендуемый набор:
```json
{
  "mcpServers": {
    "context7": {
      "url": "https://mcp.context7.com/mcp"
    },
    "Playwright": {
      "command": "npx -y @modelcontextprotocol/server-playwright"
    }
  }
}
```

## Шаг 4: Установка команд

Если выбраны команды:

```bash
mkdir -p ~/.claude/commands  # или .claude/commands
cp /path/to/ai-tools/commands/*.md ~/.claude/commands/
```

Доступные команды:
- `c-implement-feature` — полный цикл разработки
- `c-implement-tdd-feature` — TDD workflow
- `c-implement-bdd-feature` — BDD workflow
- `c-implement-s-feature` — простая реализация
- `c-implement-s-tdd-feature` — простой TDD
- `c-implement-s-bdd-feature` — простой BDD

## Шаг 5: Установка скиллов

Если выбраны скиллы, покажи список и дай выбрать:

| Скилл | Описание | Зависимости |
|-------|----------|-------------|
| `create-game-assets` | Генерация ассетов DALL-E 3 | OPENAI_API_KEY |
| `unity-assets` | Unity материалы и шейдеры | OPENAI_API_KEY |
| `poc-hypothesis` | Быстрый POC | — |
| `implement-tdd-feature` | TDD workflow | — |
| `remove-background` | Удаление фона | rembg |

Для каждого выбранного скилла:
```bash
cp -r /path/to/ai-tools/skills/SKILL_NAME ~/.claude/skills/
cd ~/.claude/skills/SKILL_NAME && npm install  # если есть package.json
```

## Шаг 6: Переменные окружения

Напомни о необходимых переменных:

```bash
# Добавить в ~/.zshrc или ~/.bashrc

# GitHub MCP
export GITHUB_PERSONAL_ACCESS_TOKEN="ghp_..."

# OpenAI (для скиллов)
export OPENAI_API_KEY="sk-..."
```

## Шаг 7: Верификация

После установки проверь:
1. `claude --version` — Claude Code установлен
2. Покажи список установленных компонентов
3. Предложи перезапустить терминал для применения env vars

## Результат

Выведи итоговую сводку:
- ✅ Установленные компоненты
- ⚠️ Требуемые env vars
- 📝 Следующие шаги
